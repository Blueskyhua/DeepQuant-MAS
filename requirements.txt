# 基础框架与日志
logging

# 数据获取与处理
pandas==2.2.0
numpy==1.26.0
yfinance==0.2.36  # 获取股票/加密货币历史数据

# 深度学习与计算 (Execution Agent)
torch==2.2.0      # 如果使用 PyTorch 运行 Transformer
scikit-learn==1.4.0

# 大模型交互 (Reasoning Agent)
openai==1.12.0    # 兼容大多数 LLM API 协议 (如 DeepSeek, GPT 等)
requests==2.31.0  # 用于基础的 API 请求

# 环境管理
python-dotenv==1.0.1
